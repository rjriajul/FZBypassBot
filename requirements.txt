aiohttp
bs4
cloudscraper
curl_cffi
flask
lxml
python-dotenv
requests
uvloop
wzgram
# playwright  # Optional: install manually for JS-rendered bypass support
#             # NOT recommended on free-tier deployments (adds ~280MB RAM)
#             # To enable: pip install playwright && playwright install chromium
